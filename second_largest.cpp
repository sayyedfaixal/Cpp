#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a[] = {12, 35, 1, 10, 34, 1};
    unordered_set<int> setA;
    int large, sec_large;
    for (int i : a)
    {
        setA.insert(i);
    }
    return 0;
}